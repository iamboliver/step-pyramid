# This file has NO effect on real crawl behaviour: it deploys as part of
# the step-pyramid site, so it only lives at
# https://oliverbarwell.com/step-pyramid/robots.txt -- not at the zone root
# (https://oliverbarwell.com/robots.txt), which is the only location a
# crawler ever checks. That path is already serving oliverbarwell.com's own
# robots.txt (Cloudflare's managed "Content Signals" file, generated at the
# zone level, outside this repo). Keeping this file for documentation only.
#
# Worth knowing: that zone-level file currently Disallows several AI
# crawlers site-wide (GPTBot, ClaudeBot, Google-Extended, CCBot, and
# others) via Content-Signal: ai-train=no, while still allowing regular
# search indexing (Googlebot, Bingbot, etc. are not blocked). That's a
# domain-wide policy set outside this repo, not something to change here --
# flagging in case it's not the intended setting for this specific page (it
# means this solver won't surface in AI-generated answers/summaries that
# respect those signals, only in regular search results).

User-agent: *
Allow: /

Sitemap: https://oliverbarwell.com/step-pyramid/sitemap.xml
